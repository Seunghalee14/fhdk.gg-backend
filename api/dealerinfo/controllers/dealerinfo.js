"use strict";

/**
 * Read the documentation (https://strapi.io/documentation/developer-docs/latest/development/backend-customization.html#core-controllers)
 * to customize this controller
 */

module.exports = {
  async create(ctx) {
    // let request = ctx.request.body;
    // let fin = [];
    // request.forEach((element) => {
    //   fin.push({
    //     name: element.name,
    //     continents: [element.continent_id],
    //     itemfavors: element.favor_ids,
    //     itemcards: element.card_ids,
    //     itemextras: [element.spice_ids],
    //   });
    // });
    // console.log(fin);
    // fin.forEach((element) => {
    //   strapi.services.dealerinfo.create(element);
    // });
    // return sanitizeEntity(entity, { model: strapi.models.itemfavor });
  },
};
